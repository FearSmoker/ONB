import React from "react";
import ReactDOM from "react-dom/client";
import OnbMail from "./components/OnbMail";
import "./components/onb-mail.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <OnbMail />
  </React.StrictMode>
);
